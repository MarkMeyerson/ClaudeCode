# Database (Supabase Transaction Pooler - Port 6543)
# IMPORTANT: Use port 6543 (transaction pooler) for serverless functions, NOT port 5432
# IMPORTANT: URL-encode special characters in password (@ becomes %40, # becomes %23, etc.)
DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:6543/postgres

# Microsoft Graph API (Email via Azure AD)
# Create an Azure AD app with Mail.Send permissions
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id
AZURE_CLIENT_SECRET=your-client-secret
M365_SENDER_EMAIL=info@sherpatech.ai

# HubSpot CRM (Optional)
# Get API key from HubSpot Settings > Integrations > API Key
HUBSPOT_API_KEY=your-hubspot-api-key
